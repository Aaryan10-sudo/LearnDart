//Maps In Dart
/*Maps Are key value function */
void main() {
  //Starting Map Journey
  /*
  var test = {
    "Name": "Aaryan",
    "Gender": "Male"
  }; //"Name" and "Gender" are key whereas "Aaryan" and "Male" is a value and {"Name": "Aaryan", "Gender" : "Male"} is a item
  print(test); //test will print the item
  print(test["Name"]); //test[key] will print the value stored in the key "Name"
  print(test[
      "Gender"]); //test[key] will print the value stored in the key "Gender"
  print(test
      .values); //The test.values will print all the values stored in the variable test
  print(test
      .keys); //The test.keys will print all the keys stored in the variable test
      

  // length in map
  print(test
      .length); //test.length will print the number of items stored in the variable test

  // Modifying Map

  test["Age"] = "20"; //This will add a new key-value pair to the map.
  print(test);
  test.addAll({
    "Hobby": "</>",
    "Aim": "Software Engineer"
  }); //test.addAll helps in adding more then one key-value pair to the map
  print(test);

  test.remove(
      "Hobby"); //The test.remove(key) will remove the key you selected from the map ;
  print(test);

  test.clear(); //The test.clear() will remove all the key-value pairs from the map;
  print(test);
  */
}
